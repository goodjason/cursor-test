# AGENTS.md

## Cursor Cloud specific instructions

This is a blank starter repository (`cursor-test`) with no application code, dependencies, or services.

- **No build / lint / test commands** exist yet. Future agents should check for `package.json`, `requirements.txt`, `Makefile`, or similar before attempting any build or test step.
- **No services to start.** There are no backend, frontend, or database services configured.
- When application code is added, update this section with the relevant dev-server, lint, test, and build commands.
